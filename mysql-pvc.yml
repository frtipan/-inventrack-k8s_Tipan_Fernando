apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: inventrack-mysql-pvc
  namespace: inventrack-prod
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: standard
